<%
  const pageTitle = "Auditoria";
  const activeMenu = "audit";
%>
<%- include("../partials/header", { pageTitle }) %>
<%- include("../partials/sidebar", { pageTitle, activeMenu }) %>

<section class="mb-6 rounded-xl bg-white p-4 shadow-sm ring-1 ring-gray-100">
  <form method="GET" action="/audit" class="grid gap-3 md:grid-cols-3">
    <label class="app-label normal-case text-gray-500">
      Usu&aacute;rio
      <select name="user_id" class="mt-1 w-full rounded-lg border border-gray-300 px-3 py-2">
        <option value="">Todos</option>
        <% users.forEach((user) => { %>
          <option value="<%= user.id %>" <%= Number(selectedUserId) === Number(user.id) ? "selected" : "" %>><%= user.name %></option>
        <% }) %>
      </select>
    </label>
    <label class="app-label normal-case text-gray-500">
      A&ccedil;&atilde;o
      <select name="action" class="mt-1 w-full rounded-lg border border-gray-300 px-3 py-2">
        <option value="">Todas</option>
        <% actions.forEach((item) => { %>
          <option value="<%= item.action %>" <%= selectedAction === item.action ? "selected" : "" %>><%= item.action %></option>
        <% }) %>
      </select>
    </label>
    <div class="flex items-end">
      <button type="submit" class="w-full rounded-lg bg-indigo-600 px-4 py-2 text-sm font-medium text-white transition-all duration-200 hover:bg-indigo-700 hover:shadow-md">Filtrar</button>
    </div>
  </form>
</section>

<section class="overflow-hidden rounded-xl bg-white shadow-sm ring-1 ring-gray-100">
  <div class="overflow-x-auto">
    <table class="min-w-full divide-y divide-gray-200">
      <thead class="bg-gray-50">
        <tr>
          <th class="app-label px-4 py-3 text-left">Usu&aacute;rio</th>
          <th class="app-label px-4 py-3 text-left">A&ccedil;&atilde;o</th>
          <th class="app-label px-4 py-3 text-left">Descri&ccedil;&atilde;o</th>
          <th class="app-label px-4 py-3 text-left">Data/Hora</th>
        </tr>
      </thead>
      <tbody class="divide-y divide-gray-100 bg-white">
        <% if (logs.length === 0) { %>
          <tr>
            <td class="px-4 py-4 text-sm text-gray-500" colspan="4">Nenhum registro encontrado.</td>
          </tr>
        <% } %>
        <% logs.forEach((log) => { %>
          <tr>
            <td class="px-4 py-4">
              <p class="text-sm font-medium text-gray-900"><%= log.user_name || "Sistema" %></p>
              <p class="mt-0.5 text-xs text-gray-500">ID #<%= log.id %></p>
            </td>
            <td class="px-4 py-4 text-sm text-gray-500"><%= log.action %></td>
            <td class="px-4 py-4 text-sm text-gray-500"><%= log.description || "-" %></td>
            <td class="px-4 py-4 text-sm text-gray-500"><%= new Date(log.created_at).toLocaleString("pt-BR") %></td>
          </tr>
        <% }) %>
      </tbody>
    </table>
  </div>
</section>

<%- include("../partials/footer") %>

